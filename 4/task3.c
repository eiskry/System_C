#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
int seed;

if (argc != 2) {
    fprintf(stderr, "Missing file argument\n");
 return 1;
  }

seed = atoi(argv[1]);
srand(seed);

for(int i =0; i<)


}
